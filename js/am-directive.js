[].forEach.call(document.querySelectorAll('[data-am]'), function(el) {
	console.log(el);
});


 		// var states = scope.am.states,
 		// 	triggers = scope.am.triggers || [];

 		// var musician = Object.create(am.maestro);
 		// musician.init({
 		// 	triggers: triggers,
 		// 	states: states,
 		// 	element: element
 		// });			

 		// musician.changeState('default');
